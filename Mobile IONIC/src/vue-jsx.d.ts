// Ionic web components use the Custom Elements API, so we declare them here
// to prevent Volar/vue-tsc from raising JSX IntrinsicElements 'any' errors.
// This extends the global JSX namespace so every ion-* tag is accepted.
declare namespace JSX {
  interface IntrinsicElements {
    [tagName: string]: unknown
  }
}
